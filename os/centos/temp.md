- Check Apache version  
`$ /usr/sbin/apache2 -v`  
`rpm -q httpd`  
`yum list httpd`


http://unix.stackexchange.com/

- What operating system is my server running?
  - CentOS
  `cat /etc/*release*`
  - Ubuntu
  `lsb_release -a`
